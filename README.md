# Network Diagnostic Tool

Python network diagnostic tool for Linux. Enumerates interfaces, 
resolves DNS, checks reachability, parses default gateway, 
and scans local ports.

## Usage
python3 network_diagnostic.py

## Requirements
Linux, Python 3.8+
